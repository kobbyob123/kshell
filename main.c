#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>

int main(){
    int flag = 1;

    while (flag == 1){
        printf("kobby/osh --> ");
        // get some characters from standard input
        // split them
        // create a fork
        // run an execve(2) to run the args
        // handle errors
        // look out for exit codes
    }
    return (0);
}
